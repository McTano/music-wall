<% @tracks.each do |track| %>
  <div class="tracks">
    <a href="<%= track.url || 'https://www.google.com/' %>"><h3><%= track.title %></a> <small>(<%= track.author %>)</small></h3>
  </div>
<% end %>
<a href="/tracks/new">Post a sweet track.</a>